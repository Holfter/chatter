import {Box, styled} from '@mui/material'

export const RowFlexBox = styled(Box)(() => ({
  display: 'flex',
}))

export const ColumnFlexBox = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
}))
